# ========================
# Server Configuration
# ========================
server.port=8080

# ========================
# Database Configuration
# ========================
spring.datasource.url=jdbc:mysql://localhost:3307/fraud_detection_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sidraNoor12
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ========================
# JWT Configuration
# ========================
app.jwt.secret=fraud_detection_super_secret_key_2024_very_long_and_secure_key_at_least_256_bits
app.jwt.expiration=86400000

# ========================
# Email Configuration (Gmail)
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sn0255724@gmail.com
spring.mail.password=wdiqhueakbyonkhs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ========================
# ML Service
# ========================
ml.service.url=http://localhost:8000

# ========================
# CORS
# ========================
app.cors.allowed-origins=http://localhost:4200

# ========================
# Simulation
# ========================
simulation.auto-start=false
simulation.interval-ms=3000
